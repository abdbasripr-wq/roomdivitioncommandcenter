import { LoginForm } from '@/components/auth/login-form';

export const metadata = {
  title: 'Login - Room Division Command Center',
  description: 'Sign in to your hotel analytics dashboard',
};

export default function LoginPage() {
  return <LoginForm />;
}
